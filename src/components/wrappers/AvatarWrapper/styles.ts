import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles({
    avatarWrapper: {
        display: "flex",
        justifyContent: "center",
        marginBottom: 38
    }
})