import {styled} from "@mui/material";

export const PickersWrapper = styled("div")({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 24,
    fontSize: 18,
    fontWeight: 700
})