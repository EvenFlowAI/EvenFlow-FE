import React from 'react';
import Agent from '../../../features/admin/Agent/Agent';

const ConfigurationAgent = () => {
  return (
    <React.Fragment>
      <Agent agentName="Configuration AI Agent" />
    </React.Fragment>
  );
};

export default ConfigurationAgent;
