import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    container: {
        width: "100%",
        marginTop: 20
    },
    panel: {
        padding: "20px 0"
    }
});
