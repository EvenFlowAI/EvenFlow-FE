export type TPasswordForm = {
    oldPassword: string;
    newPassword: string;
    repeatPassword: string;
}

export type TForm = {
    firstName: string;
    lastName: string;
    phoneNumber: string;
};
