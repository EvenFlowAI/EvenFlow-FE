import {styled} from "@material-ui/core";

export const OpsCode = styled('div')({
    height: 60,
    fontSize: 40,
    color: '#000000',
    fontWeight: 'bold',
    marginBottom: 30,
})

export const InputWrapper = styled('div')({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    fontSize: 12,
    fontWeight: 'bold',
    marginBottom: 30
})