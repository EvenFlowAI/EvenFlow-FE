import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
    scrollableTable: {
        maxHeight: 300,
        overflowY: 'auto',
        marginBottom: 20,
    }
}))