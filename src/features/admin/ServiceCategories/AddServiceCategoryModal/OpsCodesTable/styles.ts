import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
    scrollableTable: {
        maxHeight: 300,
        overflowY: 'auto',
        marginBottom: 20,
    }
}))