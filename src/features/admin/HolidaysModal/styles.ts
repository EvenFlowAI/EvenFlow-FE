import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    divider: {
        margin: "0 !important"
    },
    addHoliday: {
        textAlign: "right",
        marginBottom: 5,
        marginRight: 15
    }
});