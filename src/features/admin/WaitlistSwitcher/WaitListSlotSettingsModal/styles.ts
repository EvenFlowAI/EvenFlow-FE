import { makeStyles } from 'tss-react/mui';

//
export const useStyles = makeStyles()(() => ({
    inputWrapper: {
        marginBottom: 24
    },
    cancelButton: {
        color: '#9FA2B4'
    },
}));