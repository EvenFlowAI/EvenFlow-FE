import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
    inputWrapper: {
        marginBottom: 24
    },
    cancelButton: {
        color: '#9FA2B4'
    },
}))