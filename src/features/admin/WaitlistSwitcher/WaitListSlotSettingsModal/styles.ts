import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
    inputWrapper: {
        marginBottom: 24
    },
    cancelButton: {
        color: '#9FA2B4'
    },
}))