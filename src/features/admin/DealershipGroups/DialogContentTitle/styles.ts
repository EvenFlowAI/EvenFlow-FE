import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles({
    dialogContentTitle: {
        fontSize: 19,
        lineHeight: "16px",
        fontWeight: "bold",
        marginBottom: 22
    },
})