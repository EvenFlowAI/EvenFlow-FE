import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    dialogContentTitle: {
        fontSize: 19,
        lineHeight: "16px",
        fontWeight: "bold",
        marginBottom: 22
    },
})