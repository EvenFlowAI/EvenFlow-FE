import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
    filtersWrapper: {
        display: 'flex',
        alignItems: 'center',
    },
    autocomplete: {
        marginLeft: 20,
    }
}))