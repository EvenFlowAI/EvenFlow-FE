version: 0.2
    
phases:
  pre_build:
    commands:
      - echo Restore started on 'date'
      - yarn install
      
  build:
    commands:
      - echo Publish started on 'date'
      - yarn start
