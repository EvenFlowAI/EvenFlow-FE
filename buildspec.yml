version: 0.2
    
phases:
  install:
    runtime-versions:
      nodejs: 18

  pre_build:
    commands:
      - yarn install

  build:
    commands:
      - yarn run buildQA

artifacts:
  files:
    - build/**/*
    - deploy-scripts/*
    - appspec.yml