version: 0.0
os: linux

files: 
  - source: build
    destination: /home/ubuntu/evenflow/client

hooks:
    BeforeInstall:
      - location: deploy-scripts/before_install.sh
        timeout: 120
        runas: root